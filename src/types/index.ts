export interface NavLink {
  href: string;
  label: string;
}

export interface Card {
  img: string;
  title: string;
  description: string;
}

export interface PricingCard {
  title: string;
  price: string;
  description: string;
}
